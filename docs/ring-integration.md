# Ring integration

The `RingCameraProvider` is intentionally a stub. Enabling it requires an approved official Ring developer integration, client ID/secret supplied through secret management, OAuth redirect/webhook configuration, event subscription permission, device-to-household mapping, and documented permission to retrieve event media. No unofficial Ring library or undocumented endpoint is used. Keep provider media IDs only; do not archive clips.
