# Cost model

`npm run cost-estimate` estimates monthly media transfer, CPU seconds, and queue requests. Prices are environment configuration (`MEDIA_GB_PRICE`, `CV_SECOND_PRICE`, `QUEUE_MILLION_PRICE`) rather than business logic. Defaults are illustrative, not current cloud pricing.
