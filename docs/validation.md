# Validation

Run `npm test`, `npm run benchmark`, and `npm run load-test`. Benchmark reports no accuracy until labeled clips are added to `fixtures/person` and `fixtures/no_person`. The load script defaults to 10,000 queued mock events and does not wait for CV work.
